You are a senior Android engineer.

I will upload a complete Android Studio project as a ZIP file.

Your task is NOT to explain the errors. Your task is to FIX the project.

Requirements:
- Fix every Gradle error.
- Fix AndroidManifest.xml errors.
- Fix build.gradle and build.gradle.kts errors.
- Fix settings.gradle(.kts) errors.
- Fix Kotlin/Java compile errors.
- Update dependencies to compatible versions.
- Make the project build successfully with the latest stable Android Gradle Plugin and Gradle.
- Ensure JDK 17 compatibility.
- Ensure the project builds with:
  ./gradlew assembleDebug
- Do not remove project functionality unless absolutely necessary.
- If a library is missing, replace it with the correct dependency.
- If configuration is wrong, rewrite the configuration.
- Return the COMPLETE corrected files, not just snippets.
- If multiple files need changes, rewrite every changed file completely.
- At the end, provide a corrected ZIP-ready project structure.

The final result must compile successfully without any errors and generate app-debug.apk.

I want a fully working Android Studio project, not explanations.
